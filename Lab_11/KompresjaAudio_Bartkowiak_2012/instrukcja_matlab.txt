£adowanie sygna³u z pliku
-----------
[x,Fs] = wavread('loussier.wav');


sygna³ mono
-----------
x = x(:,1);


skrócenie sygna³u do N sekund
-----------
x = x(1:N*Fs);


ods³uchiwanie sygna³u
-----------
sound(x,Fs)
soundsc(x,Fs)


ogl¹danie spektrogramu
------------
specgram(y,2048,Fs,1920); caxis([-70 20])


kodowanie subpasmowe
-----------
[y,bps]=kodowanie_subpasmowe(x,M,Q,S);

x - sygna³ oryginalny
M - lilczba subpasm
Q - liczba bitów kwantyzatora:
	pojedyncza wartoœæ - wspólny kwantyzator dla wszystkich pasm
	wektor liczb - kwantyzacja indywidualnie definiowana dla pasm 1...M
	(gdy podamy <M wartoœci, ostatnia zostanie powtórzona)
S - wspó³czynnik skaluj¹cy dla pasm:
	brak - pasma zostan¹ osobno znormalizowane przed poddaniem kwantyzacji
	wektor liczb - skalowanie indywidualnie definiowa dla pasm 1...M
	(gdy podamy <M wartoœci, ostatnia zostanie powtórzona)
	pojedyncza wartoœæ S~=1 - kolejne pasma zostan¹ przeskalowane wg S^m
y - zdekodowany sygna³
bps - szacowana œrednia liczba bitów na próbkê


Uwaga: funkcja dzia³a doœæ wolno dla ma³ej liczby pasm i d³ugich plików


przyk³adowe eksperymenty (po ka¿dym eksperymencie nale¿y sprawdziæ wartoœæ b,
				obejrzeæ spektrogram y i pos³uchaæ x oraz y):

[y,b]=kodowanie_subpasmowe(x,4,6);  % uwaga - warto skróciæ sygna³ ze wzglêdu na czas obliczeñ
[y,b]=kodowanie_subpasmowe(x,32,6);
[y,b]=kodowanie_subpasmowe(x,32,4);
[y,b]=kodowanie_subpasmowe(x,32,[8,8,8,6,4,3,0],1);
[y,b]=kodowanie_subpasmowe(x,32,[16:-1:4],1.1);




kodowanie transformatowe
-----------
[y,bps] = kodowanie_transformatowe(x,len,q,sf)
x - sygna³ oryginalny
len - d³ugoœæ bloku transformaty
Q - liczba bitów kwantyzatora:
	pojedyncza wartoœæ - wspólny kwantyzator dla wszystkich wspó³czynników Fk
	wektor liczb - kwantyzacja indywidualnie definiowana dla wspó³czynników 1...len/2
	(gdy podamy <len/2 wartoœci, ostatnia zostanie powtórzona)
S - wspó³czynnik skaluj¹cy dla wspó³czynników:
	brak - wspó³czynniki zostan¹ osobno znormalizowane przed poddaniem kwantyzacji
	wektor liczb - skalowanie indywidualnie definiowa dla k = 1...len/2
	(gdy podamy <len/2 wartoœci, ostatnia zostanie powtórzona)
	pojedyncza wartoœæ S~=1 - kolejne wspó³czynniki zostan¹ przeskalowane wg S^k
y - zdekodowany sygna³
bps - szacowana œrednia liczba bitów na próbkê



przyk³adowe eksperymenty (po ka¿dym eksperymencie nale¿y sprawdziæ wartoœæ b,
				obejrzeæ spektrogram y i pos³uchaæ x oraz y):

[y,b]=kodowanie_transformatowe(x,64,5);
[y,b]=kodowanie_transformatowe(x,256,5);
[y,b]=kodowanie_transformatowe(x,2048,5);
[y,b]=kodowanie_transformatowe(x,8192,5);
[y,b]=kodowanie_transformatowe(x,256,6,1.05);

